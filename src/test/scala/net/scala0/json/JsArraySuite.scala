package net.scala0.json

import org.scalatest.FunSuite

class JsArraySuite extends FunSuite {
    /*
    test("JsArray slice") {
        val array = JsArray(1, 2, 3, 4)
        assert(array.drop(1) === JsArray(2, 3, 4))
        assert(array.take(2) === JsArray(1, 2))
        assert(array.slice(1, 3) === JsArray(2, 3))
    }
    
    test("JsMutableArray slice") {
        val array = JsArray(1, 2, 3, 4)
        assert(array.drop(1) === JsArray(2, 3, 4))
        assert(array.take(2) === JsArray(1, 2))
        assert(array.slice(1, 3) === JsArray(2, 3))
    }
    */
}
